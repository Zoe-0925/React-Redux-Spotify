describe('toggleTrack', () => {
    it('renders correctly', () => { })
})

describe('toggleAlbum', () => {
    it('renders correctly', () => { })
})

describe('toggleArtist', () => {
    it('renders correctly', () => { })
})

describe('togglePlaylist', () => {
    it('renders correctly', () => { })
})

describe('watchFetchNextTracks', () => {
    it('renders correctly', () => { })
})

describe('watchFetchNextTracks', () => {
    it('renders correctly', () => { })
})